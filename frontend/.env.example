# VITE_API_BASE_URL: Base URL of the MediGuard AI backend API.
# Vite only exposes env vars prefixed with VITE_ to client code.
# Defaults to "/api" (see src/lib/axios.js) if not set, which assumes
# the backend is reverse-proxied under the same origin during dev/prod.
VITE_API_BASE_URL="/api"
